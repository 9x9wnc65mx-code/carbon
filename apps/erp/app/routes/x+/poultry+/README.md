# Poultry route tree

This route tree intentionally uses Carbon's existing authenticated `/x` shell, `GroupedContentSidebar`, route handles and permission gates.

Current foundation routes:
- `/x/poultry`
- `/x/poultry/farms`
- `/x/poultry/flocks`

Data-backed CRUD and the Flock Digital Passport follow after the foundation passes CI.
